# Web Form with Authorization (PHP + MySQL)

## 📄 Описание
Проект реализует веб-форму с:
- автоматической генерацией логина и пароля при первом сохранении;
- сохранением данных в MySQL с хешированием пароля;
- авторизацией через логин/пароль и редактированием данных;
- поддержкой куки и сессий;
- сохранением любимых языков программирования (multiple select).

## 📂 Файлы
- `index.php` — главная точка входа, логика регистрации/редактирования.
- `login.php` — форма авторизации.
- `logout.php` — выход из сессии.
- `form.php` — HTML-форма (с подстановкой значений).
- `index.html` — старый шаблон формы (не используется, для справки).

## 🛠️ Подключение к базе
Настройки подключения заданы в каждом PHP-файле:
- хост: `localhost`
- имя базы: `u68806`
- пользователь: `u68806`
- пароль: `1921639`

## 📌 Важно
Таблица `application` должна содержать следующие поля:
```sql
id INT AUTO_INCREMENT PRIMARY KEY,
fio VARCHAR(150),
phone VARCHAR(20),
email VARCHAR(100),
dob DATE,
gender VARCHAR(10),
bio TEXT,
languages TEXT,
login VARCHAR(50),
password_hash VARCHAR(255)
```

## 🚀 Запуск
Разместите проект в корне веб-сервера (например, `/var/www/html`) и откройте `index.php`.